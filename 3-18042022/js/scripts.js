function tabuada(){
    var n = prompt("entre com número para fazer a tabuada");
    var rs = "";
    let x = 0;
    for(let i = 0 ; i <=10 ; i++){ 
        rs += n + " x " + " i " + " = " + (n*i);
    }
    return rs;
}

function retangulo(){
    var saida = "";
        for (let linha = 0 ; linha <= 30 . linha++)
        {
            for (let coluna = 0 ; coluna <= 50 . coluna++){
                saida+="";
            }
            saida+="<br>";
        }
        document.getElementById("painel").incertHTML=saida;
}

function triangulo(){
    var saida = "";
       for (let linha = 0 ; linha <= 30 . linha++){
           for (let coluna = 0 ; coluna <= 50 . coluna++
            
            
            ){
               saida+= "<br>"
           }
           saida+= "<br>";
       }
       document.getElementById("painel").incertHTML=saida;
}

function


